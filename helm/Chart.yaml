# Helm chart metadata for LDAPGuard.
# Versioning is intentionally aligned with VERSION and pyproject.toml.
apiVersion: v2
name: ldapguard
description: A Helm chart for LDAPGuard
type: application
version: 1.0.0
appVersion: "1.0.0"
